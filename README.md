# curator

Единственный писатель канона памяти секретаря (репо `panelmem-kb`).

Агенты память только читают (`memory_search` + baseline на старте). Кнопки save
у них нет. Куратор смотрит на следы всех голов, поэтому работает одинаково для
Claude / Hermes / Codex.

## Что делает

- **Вход:** переписки всех голов (парсер на голову) + родные файлы агентов
  (CLAUDE.md / GEMINI.md / USER.md).
- **Экстракция:** дешёвой моделью вытаскивает durable-факты (формат «утверждение + почему»).
- **Дедуп/конфликты:** near-dup → skip/merge, конфликт → supersede старого.
- **Redaction:** сырые секреты из транскриптов не попадают в канон (git вечен).
- **Запись:** коммитит markdown в `panelmem-kb`. Индекс не трогает — его ребилдит memory-mcp.
- **Baseline:** обновляет `baseline.md` в `panelmem-kb` (его импортирует персона секретаря).

## Инварианты

- Один сериализованный демон с локом/очередью на канон, НЕ спавн по сессии
  (иначе несколько писателей в общий git-канон).
- Триггер: on-session-end + часовой sweep как страховка (упавшие сессии).
- Watermark/курсор по источнику: обрабатываем только новое.
- Себя не харвестит. Бежит на дешёвой голове/подписке.

## Статус

Скелет. Реализация — в бэклоге (см. control-panel). Дизайн: `control-panel/docs/secretary.md`.
